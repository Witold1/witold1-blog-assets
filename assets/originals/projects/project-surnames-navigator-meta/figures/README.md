# figures/

Auxiliary media for this content slug only: diagrams, banners, pipeline sketches, MDX insets — not the primary gallery/visualization asset.

Use short role filenames (`dag.svg`, `banner.jpeg`). See the repo README (Primary vs `figures/`).
